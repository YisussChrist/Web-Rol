# Revisión del 7 de septiembre de 2026

## Organización

- Añadido `README.md` con mapa de las carpetas, instrucciones de apertura,
  documentación de las copias históricas y puntos de entrada de cada sección.
- Extraído el bloque de estilos de `juegos/index.html` a `assets/css/juegos.css`.
- Agrupada la nueva interacción del catálogo en `assets/js/juegos.js`.
- Movido `pruebatraductor.py` a `scripts/traductor-pantalla.py`, sin modificar
  su contenido. Se inicia con `python scripts/traductor-pantalla.py` y necesita
  las mismas dependencias externas que antes.
- Eliminado `.github/workflows/.txt`: solo contenía la letra «a» y no era un flujo.
- Añadido `scripts/audit_site.py` y su informe `docs/auditoria-enlaces.json`.
- Ampliada `GUIA-DESARROLLO.md` con las rutas y comprobaciones nuevas.

## Mejoras del menú de juegos

- Agrupación real por estado: cuatro jugables (incluida la beta), dos en
  desarrollo y diez futuros. Antes cuatro de los futuros aparecían entre
  los disponibles y su contador indicaba once.
- Buscador por texto, sin distinguir mayúsculas ni tildes.
- Filtros combinables con la búsqueda y contadores calculados desde las tarjetas.
- Mensaje sin resultados y botón para restablecer el catálogo.
- Sustituidos los enlaces vacíos de juegos futuros por etiquetas sin navegación.
- Corregidos dos valores CSS inválidos y el selector móvil `.hero.card`.
- El enlace de notas de la beta ahora tiene apariencia de enlace utilizable.
- Añadidos foco visible, salto al catálogo, estado accesible de los filtros,
  anuncio de resultados, estructura `main` y respeto al movimiento reducido.
- Textos orientados a quien va a jugar, descripción de página y corrección
  de «descrubrir» por «descubrir».
- Catálogo completo disponible cuando JavaScript está desactivado.

## Comprobaciones

- Auditoría recursiva de referencias estáticas HTML/CSS: ninguna referencia
  local ausente en los archivos analizados; el informe registra el alcance.
- Comprobación de sintaxis de los archivos JavaScript versionados y del nuevo
  archivo del catálogo.
- Pruebas con Edge: 16 tarjetas iniciales, los tres filtros de estado,
  búsqueda «quien», búsqueda sin resultados y restablecimiento.
- Vistas de 390 y 1280 píxeles sin desbordamiento horizontal y capturas guardadas
  en `docs/juegos-390.png` y `docs/juegos-1280.png`.
- Sin errores JavaScript en esa navegación y 16 tarjetas accesibles sin JavaScript.

La revisión global es estructural y estática; no equivale a probar cada juego,
reproductor, servicio externo o bot. No se ha publicado ni enviado ningún aviso
a Discord. Las copias históricas, datos personales y recursos multimedia se
han conservado.
